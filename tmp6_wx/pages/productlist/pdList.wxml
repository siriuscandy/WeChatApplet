<view class="content bgfff">
<view class="find_nav">
            <view class="find_nav_left">
                <view class="find_nav_list" id="pagenavi1">
                    <ul>
                        <li><a href="#" class="active" >{{cateName}}</a></li>
                    </ul>
                </view>
            </view>
        </view>
        <view  class="swipe">
              <view class="box01_list">
                <view class="li_list" >
                	<view class="mui-table-view mui-grid-view" >
                        <view class="mui-table-view-cell mui-media mui-col-xs-6" wx:for="{{prolist}}" wx:key="productlist">
                            <navigator url="../productDetails/productDetails?goodsId={{item.id}}">
                                <image class="mui-media-object" src="{{item.cover}}" />
                                <view class="mui-media-body" >{{item.name}}</view>
                                <view class="mui-media-body jgPice">¥<d>{{item.price}}</d>/次</view>
                            </navigator>
                        </view>

                     </view>
                </view>

              </view>
        
            </view>
</view>